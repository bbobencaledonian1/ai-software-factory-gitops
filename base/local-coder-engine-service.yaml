apiVersion: v1
kind: Service
metadata:
  name: local-coder-engine
  namespace: ai-factory-local
  annotations:
    argocd.argoproj.io/tracking-id: 'ai-software-factory:/Service:ai-factory-local/local-coder-engine'
spec:
  ports:
    - name: http
      port: 11434
      targetPort: 11434
      protocol: TCP
  selector:
    app: local-coder-engine
  type: ClusterIP
