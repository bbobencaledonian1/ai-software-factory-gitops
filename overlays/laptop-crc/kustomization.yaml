apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-factory-local

resources:
  - ../../base

patches:
  - path: patches/laptop-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: local-coder-engine
      namespace: ai-factory-local
